{
  "name": "bold-vulnerable-demo-app",
  "version": "1.0.0",
  "private": true,
  "description": "A deliberately vulnerable customer portal for BOLA/IDOR testing.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "start:env": "node --env-file=.env server.js",
    "dev": "node --watch server.js",
    "dev:env": "node --env-file=.env --watch server.js",
    "test": "node scripts/smoke-test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
